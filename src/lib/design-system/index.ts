/**
 * 设计系统入口文件
 * 导出所有设计系统相关的 tokens 和工具函数
 */

export {
  colors,
  spacing,
  borderRadius,
  shadows,
  blur,
  animations,
  typography,
  zIndex,
  breakpoints,
  designTokens,
  getThemeColor,
} from './tokens';

export type { Theme } from './tokens';
